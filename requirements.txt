# ── Ingestion ───────────────────────────────────────────
confluent-kafka~=2.6          # librdkafka-backed; the production-grade client

# ── Storage clients ─────────────────────────────────────
boto3~=1.35                   # S3 API against MinIO
psycopg[binary]~=3.2          # PostgreSQL serving store
pyarrow~=17.0                 # Parquet reads outside Spark (tests, dashboard)

# ── Serving ─────────────────────────────────────────────
fastapi~=0.115
uvicorn[standard]~=0.32
streamlit~=1.39
plotly~=5.24
pandas~=2.2

# ── Config & observability ──────────────────────────────
pydantic~=2.9
pydantic-settings~=2.6
python-dotenv~=1.0
prometheus-client~=0.21
requests~=2.32